import React from 'react';
import { View, Text } from 'react-native';

const PatientFormScreen = () => (
  <View style={{ flex: 1, justifyContent: 'center', alignItems: 'center' }}>
    <Text>Patient Form Screen</Text>
  </View>
);

export default PatientFormScreen;
